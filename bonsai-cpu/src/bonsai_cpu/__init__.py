"""Run the 1-bit Bonsai models on CPU.

The package behind the `bonsai-cpu` command: what the models are, how their
runtime is found, and — for chat — a resident server and the terminal
interface that drives it.
"""

__version__ = "0.1.0"
